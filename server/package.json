{
  "name": "server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "bun --env-file=../.env --watch run src/index.ts",
    "build": "tsc"
  },
  "dependencies": {
    "shared": "workspace:*",
    "db": "workspace:*",
    "auth": "workspace:*",
    "schemas": "workspace:*",
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
