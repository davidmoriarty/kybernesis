{
  "name": "server",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "type-check": "tsc -b --pretty false",
    "test": "echo \"no tests\"",
    "start": "bun /app/server/dist/index.js",
    "dev": "bun --env-file=../.env --watch run src/dev.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1073.0",
    "auth": "workspace:*",
    "db": "workspace:*",
    "hono": "^4.12.3",
    "schemas": "workspace:*",
    "shared": "workspace:*"
  }
}
