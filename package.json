{
  "name": "kybernesis",
  "private": true,
  "types": "module",
  "version": "0.5.1",
  "author": "David Moriarty",
  "license": "MIT",
  "description": "A multi-tenant SaaS platform for software teams and individual developers, built with Bun, Hono, React, and TypeScript.",
  "homepage": "https://kybernesis.davidmoriarty.dev",
  "repository": {
    "type": "git",
    "url": "https://github.com/davidmoriarty/kybernesis.git"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json,css,yml,yaml}": "biome check --write"
  },
  "scripts": {
    "prepare": "husky",
    "precommit": "lint-staged",
    "lint": "biome check .",
    "format": "biome check . --write",
    "type-check": "turbo run type-check",
    "test": "turbo run test",
    "db:seed": "bun --cwd=packages/db run seed",
    "dev": "turbo dev",
    "build": "turbo build",
    "dev:client": "turbo dev --filter=client",
    "dev:server": "turbo dev --filter=server --parallel",
    "build:client": "turbo build --filter=client",
    "build:server": "turbo build --filter=server --parallel"
  },
  "packageManager": "bun@1.3.9",
  "engines": {
    "bun": ">=1.3.9"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.5",
    "@types/bun": "^1.3.10",
    "bun-types": "^1.3.10",
    "dotenv": "^17.3.1",
    "drizzle-kit": "^0.31.9",
    "husky": "^9.1.7",
    "lint-staged": "^16.3.2",
    "ts-prune": "^0.10.3",
    "turbo": "^2.8.13"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "keywords": [
    "bun",
    "hono",
    "react",
    "vite",
    "monorepo",
    "turbo"
  ],
  "workspaces": [
    "./server",
    "./client",
    "./packages/shared",
    "./packages/db",
    "./packages/auth",
    "./packages/schemas"
  ]
}
